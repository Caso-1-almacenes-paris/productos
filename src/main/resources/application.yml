server:
  port: 8082

spring:
  application:
    name: productos

  # 🔌 Conexión directa a tu Base de Datos Neon
  datasource:
    url: jdbc:postgresql://ep-dark-flower-acxgvu5x-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_8DrnxkMa1gcj
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update # Esto creará automáticamente tu tabla de productos en Neon
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# 📡 Conexión automática al Eureka del grupo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true